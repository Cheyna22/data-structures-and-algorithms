package linkedlist;

public class LinkedList {
    class Node<currentNode, nextNode> {
        
        // method called insert - takes newNodeValue and adds it to the head of the list with 0(1) Time.
        public void insert(int newNodeValue) {
            // add newNodeValue to head of LinkedList
        }

        public boolean includes(int existingValue) {
            // check to see if existingValue exists in the LinkedList
            if(existingValue exists in valuesInLinkedList) {
                return true;
            }
        }

        public void print(){
            // outputs all current Node values in list
        }

    }
}
